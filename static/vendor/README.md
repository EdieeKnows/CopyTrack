# Vendor Assets

Place third-party frontend assets in this directory.

* `htmx/htmx.min.js` should contain the HTMX 2.0.8 distribution file.
* `bootstrap/css/bootstrap.min.css` and `bootstrap/js/bootstrap.bundle.min.js` should contain the Bootstrap 5 build assets.
